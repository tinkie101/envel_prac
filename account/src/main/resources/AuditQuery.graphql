query AuditQuery($auditId: UUID!) {
    audit(accountId: $auditId){
        withdrawals {
            amount
            type
        }
        deposits {
            amount
            type
        }
    }
}