query AuditsQuery($auditId: UUID!) {
    audits(accountId: $auditId){
        withdrawals {
            amount
            type
        }
        deposits {
            amount
            type
        }
    }
}